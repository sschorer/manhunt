# Client

The production client (React + Vite PWA, MapLibre GL, `watchPosition` GPS,
Screen Wake Lock) is not built yet. It is the first backlog item.

Until then, a compiled static design preview is served from `../public/` and the
reference mockup lives in `../docs/mockup/`.
