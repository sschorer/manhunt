# AGENTS.md

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues for `sschorer/manhunt`, managed with the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default vocabulary: `needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`. See `docs/agents/triage-labels.md`.

### Domain docs

Multi-context: `CONTEXT-MAP.md` at the root points to `client/CONTEXT.md` and `server/CONTEXT.md`. See `docs/agents/domain.md`.
